option('enable-optimize', type : 'boolean', value : true, description : 'Add the optimizations of configure (-funroll-loops, -fomit-frame-pointer...) where the compiler takes them')
option('enable-debug', type : 'boolean', value : false, description : 'Activate debugging messages')
option('enable-simd', type : 'boolean', value : true, description : 'Compute the intervals with SSE2 instructions on x86 processors, and gaol::interval2f with SSE3 [default=yes, as configure]')
option('enable-float-intervals', type : 'boolean', value : false, description : 'Compile gaol::intervalf, and gaol::interval2f with SSE3 [default=no, as configure and CMake]')
option('enable-asm', type : 'boolean', value : true, description : 'Use assembler code [default=yes, as configure]')
option('enable-verbose-mode', type : 'boolean', value : false, description : 'Write a line on the standard error when GAOL initializes and cleans up [default=no: GAOL is silent, as with configure and CMake]')
option('enable-preserve-rounding', type : 'boolean', value : false, description : 'Restore the rounding direction after each operation, rather than leaving it upward')
option('enable-relations', type: 'string', value:'certainly', description: 'use KIND symbols of relations, with KIND being "set", "certainly" or "possibly" [default=certainly]')
option('enable-exception', type: 'boolean', value: true, description: 'raise exceptions to signal errors [default=yes]')
option('with-mathlib', type: 'combo', choices: ['apmathlib', 'crlibm', 'default'], value: 'apmathlib', description: 'The mathematical library: apmathlib (mathlib, the default, as configure), crlibm, or default, the math library of the system, with which the bounds of the elementary functions are not certified')
option('with-mathlib-include', type: 'string', value: '', description: 'The directory of the header of the mathematical library (MathLib.h, crlibm.h), as --with-mathlib-include of configure. For mathlib, given with or without with-mathlib-lib, GAOL uses the one installed there rather than building the one of 3rd/mathlib')
option('with-mathlib-lib', type: 'string', value: '', description: 'The directory of the mathematical library (libultim, libcrlibm), as --with-mathlib-lib of configure. For mathlib, given with or without with-mathlib-include, GAOL uses the one installed there rather than building the one of 3rd/mathlib')
option('with-test', type: 'boolean', value: false, description: 'Specify if test are built [default=no]')
option('check-perf', type : 'boolean', value : false, description : 'Run test performances')
option('with-doc', type: 'boolean', value: false, description: 'Build the documentation')
